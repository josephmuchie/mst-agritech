import React from 'react';
import { Typography } from 'antd';
const { Title } = Typography;
const AppSettingsPage: React.FC = () => (<div><Title level={4}>App Settings</Title><p>This section is under construction.</p></div>);
export default AppSettingsPage;
