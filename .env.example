# Copy to .env and adjust as needed. docker-compose.yml reads this automatically.

# Port core-api listens on, both inside its container and on the host.
# Change it here only — docker-compose.yml, the frontend build, and its nginx
# proxy all derive from this one value instead of hardcoding it separately.
CORE_API_PORT=8081
